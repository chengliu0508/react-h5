<view class="admin-page">
    <homeCom wx:if="{{!current}}" userinfo="{{userinfo}}"></homeCom>
    <visitCom wx:else customers="{{customers}}"></visitCom>
    <bottomTab bindcurrentChange="currentChange"></bottomTab>
</view>